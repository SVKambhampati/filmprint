# TMDB v3 API key. Get one at https://www.themoviedb.org/settings/api
# This is ONLY read by the build-time generator. It must never reach the client.
TMDB_API_KEY=

# Requests per second. TMDB tolerates ~50/s; 20 is polite and still fast.
TMDB_RPS=20
